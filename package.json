{
  "name": "gpage-typescript-comparison",
  "version": "1.0.0",
  "description": "TypeScript number comparison",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/compare.js",
    "compare": "npm run build && npm run start",
    "compare-js": "node compare.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}